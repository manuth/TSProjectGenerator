import { strictEqual } from "assert";

suite(
    "TSProjectGeneratorTest",
    () =>
    {
        test(
            "Example…",
            () =>
            {
                strictEqual(1, 1);
            });
    });
