import { strictEqual } from "assert";

suite(
    "generator-ts-project-test",
    () =>
    {
        test(
            "Example…",
            () =>
            {
                strictEqual(1, 1);
            });
    });
