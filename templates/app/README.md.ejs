# <%= name %>
<%= description %>

## Usage
### Installing `<%= name %>`
You can install `<%= name %>` using following command:

```bash
npm install -g yo <%= moduleName %>
```

### Using the Generator
You can run the generator by invoking this command on a command prompt:

```bash
yo <%= moduleName.replace(/^generator-/, "") %>
```

## Generator Output
Use this section for documenting what your generator actually will generate.