
# <%= DisplayName %>

<%= Description %>

## Usage

### Installing `<%= DisplayName %>`

You can install `<%= DisplayName %>` using following command:

```bash
npm install -g yo <%= Name %>
```

### Using the Generator

You can run the generator by invoking this command on a command prompt:

```bash
yo <%= Name.replace(/^generator-/, "") %>
```

## Generator Output

Use this section for documenting what your generator actually will generate.
